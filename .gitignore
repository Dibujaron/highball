bin/
obj/
*.user
*.suo
.vs/

# Game assemblies are referenced from the local install, never redistributed.
*.dll
*.pdb
!**/lib/**/*.dll

# Captured measurement data
*.csv

# Measurement data is normally scratch, but sessions we cite in docs are the record.
!docs/data/*.csv
